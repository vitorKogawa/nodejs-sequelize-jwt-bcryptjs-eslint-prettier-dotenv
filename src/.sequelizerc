const { resolve } = require("path");

module.exports = {
  "config": resolve(__dirname, "src", "config", "database.config.js"),
  "models-path": resolve(__dirname, "src", "database", "models"),
  "migrations-path": resolve(__diraname, "src", "database", "migrations")
};
